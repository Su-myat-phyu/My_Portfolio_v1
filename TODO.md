# TODO
- [x] Find where the "View Resume" button/link is implemented.
- [x] Update it so it opens the PDF in a new tab (not download).
- [ ] Run a production build to sanity-check (if tooling works in this environment).

